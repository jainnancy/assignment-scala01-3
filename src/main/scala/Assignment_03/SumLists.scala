/*
package Assignment_03

class SumLists {

  def sumOfLists(list1: List[Int],list2: List[Int]): x =
    for{j <- list1; k <- list2 if( list1.indexOf(j) <= list1.size)} yield (x :: (j + k))
    x
}
*/
